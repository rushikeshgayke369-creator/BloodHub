<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@android:color/white"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Blood Group Dropdown -->
        <Spinner
            android:id="@+id/spinnerBloodGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:backgroundTint="@color/black"
            android:prompt="@string/select_blood_group" />

        <!-- Blood Component Dropdown -->
        <Spinner
            android:id="@+id/spinnerBloodComponent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:backgroundTint="@color/black"
            android:prompt="@string/select_blood_component" />

        <!-- Use My Current Location -->
        <CheckBox
            android:id="@+id/checkboxUseLocation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Use My Current Location"
            android:textSize="16sp"
            android:textColor="@color/black"
            android:checked="false"
            android:layout_marginTop="16dp" />

        <!-- State -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="16dp">

            <EditText
                android:id="@+id/etState"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="State"
                android:inputType="text"
                android:backgroundTint="@color/black" />

            <ImageView
                android:id="@+id/ivStateLocation"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_location"
                android:padding="4dp"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <!-- District -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="16dp">

            <EditText
                android:id="@+id/etDistrict"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="District"
                android:inputType="text"
                android:backgroundTint="@color/black" />

            <ImageView
                android:id="@+id/ivDistrictLocation"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_location"
                android:padding="4dp"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <!-- Continue Button -->
        <Button
            android:id="@+id/btnContinue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:text="Continue"
            android:backgroundTint="@color/red"
            android:textColor="@android:color/white"
            android:paddingHorizontal="32dp"
            android:paddingVertical="8dp"
            android:fontFamily="sans-serif-medium" />
    </LinearLayout>
</ScrollView>
